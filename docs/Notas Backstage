- Comandos para validar herramientas instaladas en la ec2, en el user_data
# Verificar la versión de Node.js
  node -v
# Verificar la versión de Git
  git --version
# Verificar la versión de Yarn
  yarn --version
# Verificar que Backstage está corriendo
  ps aux | grep 'yarn dev'
# Verificar el log de user_data
  cat /var/log/user-data.log
  cat /var/log/cloud-init-output.log
# Verificar que paso 100 lineas antes y 100 despues de instalar nodey git
  grep -C 100 "########## Instalando Node.js versión 18.x usando NV" /var/log/user-data.log

# Revisar esta guia: https://www.youtube.com/watch?v=r46uFbu9wOs&t=2047s


sudo yarn --version
sudo git --version
sudo node -v
sudo npm -v
sudo docker --version


sudo yarn --version
sudo node -v
sudo npm -v


yarn --version
node -v
npm -v